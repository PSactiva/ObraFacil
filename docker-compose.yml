services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: obrafacil
      POSTGRES_USER: obrafacil
      POSTGRES_PASSWORD: obrafacil_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
